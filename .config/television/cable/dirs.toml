[metadata]
name = "dirs"
description = "A channel to select from directories"
requirements = ["fd"]

[source]
command = ["fd -t d --hidden --exclude .git"]

[preview]
command = "ls -la --color=always {}"

[keybindings]
shortcut = "f2"
